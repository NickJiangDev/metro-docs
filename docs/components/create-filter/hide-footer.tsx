import { Button, CreateFilter } from '@metro/components';

const App = () => {
  return (
    <CreateFilter showFooter={false}>
      <Button>Open</Button>
    </CreateFilter>
  );
};
export default App;
